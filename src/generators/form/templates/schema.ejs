import { z } from "zod";

export const <%= schemaName %> = z.object({
  subaction: z.literal("<%= formName %>"),
  EXAMPLE: z.string(),
});

export type <%= schemaName %>Type = z.infer<typeof <%= schemaName %>>;
