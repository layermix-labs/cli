import { expect, test } from "vitest";
import { render } from "vitest-browser-react";
import <%= componentName %> from "./<%= componentName %>";

test("renders <%= componentName %>", async () => {
  const { getByText } = render(<Input placeholder="..." />);
  await expect.element(getByText("<%= componentName %>")).toBeInTheDocument();
});
